{
  "name": "smart-allocation",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "npm --prefix server start",
    "server:install": "npm --prefix server install",
    "setup": "npm install && npm --prefix server install",
    "start": "concurrently -n API,WEB -c magenta,cyan \"npm run server\" \"npm run dev\"",
    "dev:all": "concurrently -n API,WEB -c magenta,cyan \"npm run server\" \"npm run dev\""
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-leaflet": "^4.2.1",
    "react-router-dom": "^6.26.2",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^7.6.0",
    "concurrently": "^7.6.0",
    "vite": "^4.5.5"
  }
}
